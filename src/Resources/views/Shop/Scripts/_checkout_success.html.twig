{{ setting('monsieurbiz_scripts.scripts', 'checkout_success')|replace({
    '%orderNumber%': hookable_metadata.context.order.number|default(''),
    '%orderAmount%': hookable_metadata.context.order.total|default(0),
    '%orderCurrency%': hookable_metadata.context.order.currencyCode|default(''),
})|raw }}
